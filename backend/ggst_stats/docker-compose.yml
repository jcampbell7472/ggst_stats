services:
  db:
    container_name: postgres-ggst-stats
    image: postgres:latest
    environment:
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
      PGDATA: /data/postgres
    volumes:
      - db:/data/postgres
    ports:
      - "5332:5432"
    networks:
    - db
    restart: unless-stopped

networks:
      db:
        driver: bridge
volumes:
  db: